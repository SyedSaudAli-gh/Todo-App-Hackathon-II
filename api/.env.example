# Database Configuration
# For local development: postgresql://user:password@localhost:5432/dbname
# For Neon DB: postgresql+psycopg://user:pass@host/db?sslmode=require
DATABASE_URL=postgresql+psycopg://<user>:<password>@<host>/<database>?sslmode=require

# JWT Authentication (RS256) - REQUIRED for JWT token validation
# IMPORTANT: Must be single-line format with \n escape sequences
# Generate public key from private key: openssl rsa -in private_key.pem -pubout -out public_key.pem
# Convert to single-line: awk 'NF {sub(/\r/, ""); printf "%s\\n",$0;}' public_key.pem
# Example format: "-----BEGIN PUBLIC KEY-----\nMIIBIjANBgkqhkiG...\n-----END PUBLIC KEY-----"
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n<your-public-key-content-here>\n-----END PUBLIC KEY-----"
JWT_ALGORITHM=RS256

# API Configuration
API_VERSION=v1
DEBUG=false

# CORS Configuration (Frontend URLs - comma-separated)
# For production, add your Vercel domain: https://your-app.vercel.app
CORS_ORIGINS=http://localhost:3000

# Server Configuration
HOST=0.0.0.0
PORT=8001
